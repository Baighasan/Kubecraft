# DEPRECATED - CREATED DYNAMICALLY VIA CODE
# KEPT AS REFERENCE

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {username}
  namespace: mc-{username}
  labels:
    app: kubecraft
    user: {username}