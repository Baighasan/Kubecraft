apiVersion: v1
kind: ResourceQuota
metadata:
  name: mc-compute-resources
  namespace: mc-{username}
  labels:
    app: kubecraft
    user: {username}
spec:
  hard:
    requests.cpu: "1000m"
    requests.memory: 2Gi
    limits.cpu: "1500m"
    limits.memory: 4Gi
    persistentvolumeclaims: "1"